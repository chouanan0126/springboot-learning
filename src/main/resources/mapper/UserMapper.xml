<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.UserMapper">
    <select id="getUserById" parameterType="Integer" resultType="com.example.demo.entity.User">
        SELECT *
        FROM user
        WHERE id = #{id}
    </select>
    <select id="findAllUsers" resultMap="UserWithOrderResultMap">
        SELECT *
        FROM user
        INNER JOIN user_order ON user.id = user_order.user_id
    </select>
    <resultMap type="com.example.demo.entity.User" id="UserWithOrderResultMap">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="age" property="age"/>
        <result column="gender" property="gender"/>
        <result column="phone" property="phone"/>
    </resultMap>



    <select id="insertUser">
        INSERT INTO user (name, age, gender, phone)
        VALUES (#{name}, #{age}, #{gender}, #{phone})
    </select>
    <select id="updateUser">
        UPDATE user
        SET name=#{name},
            age=#{age},
            gender=#{gender},
            phone=#{phone}
        WHERE id = #{id}
    </select>
    <select id="deleteUser">
        DELETE
        FROM user
        WHERE id = #{id}
    </select>
</mapper>
